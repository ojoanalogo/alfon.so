---
import { Image } from "astro:assets";
import { getCollection } from "astro:content";
import me from "../assets/me-2.jpg";
import PostPreview from "../components/PostPreview.astro";
import SocialMediaIcons from "../components/SocialMediaIcons.astro";
import BaseLayout from "../layouts/BaseLayout.astro";
import { sortMDByDate } from "../utils";

const MAX_POSTS = 10;
const allPosts = await getCollection("blog");
const allPostsByDate = sortMDByDate(allPosts).slice(0, MAX_POSTS);

const techStack: Record<string, string> = {
  js: "JavaScript",
  ts: "TypeScript",
  python: "Python",
  sql: "SQL",
  gpt: "LLMs, GPT-3/4, Claude, etc.",
  "next.js": "Next.js",
  aws: "AWS",
  gcp: "GCP",
  docker: "Docker",
  tf: "Terraform",
};

const projects: Array<{
  title: string;
  description: string;
  link: string;
  icon: string;
}> = [
  {
    title: "SofIA",
    description: "Registra tus gastos con un solo mensaje de WhatsApp.",
    link: "https://sofinanzas.mx",
    icon: "💸",
  },
  {
    title: "Hilitos",
    description: "Marketplaces para pequeños emprendedores, como Etsy.",
    link: "https://hilitos.app",
    icon: "🧵",
  },
  {
    title: "Wawa",
    description:
      "Framework para construir flujos conversacionales en WhatsApp. [Próximamente]",
    icon: "💬",
    link: "",
  },
];
---

<BaseLayout navbarActiveItemID="home">
  <main>
    <section id="about" class="mt-6" aria-labelledby="about-heading">
      <div
        class="bg-black/30 border border-gray-600 rounded-lg font-mono text-sm p-6"
        role="region"
      >
        <!-- Terminal header -->
        <div class="flex items-center gap-2 mb-4 pb-3 border-b border-gray-600">
          <div class="flex gap-2" role="img">
            <div class="w-3 h-3 rounded-full bg-red-500"></div>
            <div class="w-3 h-3 rounded-full bg-yellow-500"></div>
            <div class="w-3 h-3 rounded-full bg-green-500"></div>
          </div>
          <span class="text-gray-400 text-xs ml-2">https://alfon.so</span>
        </div>

        <!-- Content -->
        <div class="text-xs">
          <div class="space-y-2">
            <div class="flex items-center gap-4 mb-4">
              <Image
                src={me}
                width={500}
                height={500}
                alt="Foto de perfil"
                class="w-12 h-12 rounded-full"
              />
              <div class="flex flex-col">
                <h1 class="text-primary text-xl">
                  Alfonso <span class="text-gray-200">Reyes</span>
                </h1>
                <p class="text-gray-400 text-xs">
                  ingeniero backend / fotógrafo
                </p>
              </div>
            </div>

            <!-- Personal paragraph -->
            <div class="bg-gray-500/10 rounded-lg p-3 mb-4">
              <p class="text-gray-200 leading-relaxed">
                bienvenido a mi pequeño rincón en internet, aquí encontrarás mis
                pensamientos (pocos), mis proyectos y mi blog.
              </p>
            </div>

            <div class="border-b border-gray-600 w-full mb-2"></div>

            <div class="flex">
              <span class="text-gray-400 w-36 shrink-0">Ubicación</span>
              <span class="text-white">México 🌮🇲🇽</span>
            </div>

            <div class="flex">
              <span class="text-gray-400 w-36 shrink-0">Trabajo</span>
              <span class="text-white">
                Ingeniero Backend @
                <a
                  target="_blank"
                  rel="noopener noreferrer"
                  class="text-cyan-600 hover:underline focus:outline-none focus:ring-2 focus:ring-cyan-600 focus:ring-offset-2 focus:ring-offset-gray-900"
                  href="https://monopolio.com.mx"
                  aria-label="Visitar sitio web monopolio.com.mx"
                  >monopolio.com.mx</a
                >
              </span>
            </div>

            <div class="flex">
              <span class="text-gray-400 w-36 shrink-0">Hobby</span>
              <span class="text-white">
                Fotografía @
                <a
                  class="text-cyan-600 hover:underline focus:outline-none focus:ring-2 focus:ring-cyan-600 focus:ring-offset-2 focus:ring-offset-gray-900"
                  target="_blank"
                  rel="noopener noreferrer"
                  href="https://ojoanalogo.com"
                  aria-label="Visitar portafolio de fotografía ojoanalogo.com"
                  >ojoanalogo.com</a
                >
              </span>
            </div>

            <div class="flex items-center">
              <span class="text-gray-400 w-36 shrink-0">Proyecto actual</span>
              <span class="text-white">
                <a
                  target="_blank"
                  rel="noopener noreferrer"
                  class="text-cyan-600 hover:underline focus:outline-none focus:ring-2 focus:ring-cyan-600 focus:ring-offset-2 focus:ring-offset-gray-900"
                  href="https://sofinanzas.mx"
                  aria-label="Visitar SofIA - Asistente de finanzas con IA"
                  >SofIA</a
                >
                - Un asistente de finanzas personales para todos 💸
              </span>
            </div>

            <div class="flex">
              <span class="text-gray-400 w-36 shrink-0">Intereses</span>
              <span class="text-white"
                >Programación, startups, ciencia ficción, películas, fotografía,
                viajes</span
              >
            </div>

            <div class="flex">
              <span class="text-gray-400 w-36 shrink-0">Contacto</span>
              <span class="text-white">
                <a
                  class="text-cyan-600 hover:underline focus:outline-none focus:ring-2 focus:ring-cyan-600 focus:ring-offset-2 focus:ring-offset-gray-900"
                  href="mailto:hola@alfon.so"
                  aria-label="Enviar email a hola@alfon.so">hola@alfon.so</a
                >
              </span>
            </div>

            <div class="flex items-center">
              <span class="text-gray-400 w-36 shrink-0">Social</span>
              <span class="text-white flex-1 min-w-0">
                <SocialMediaIcons />
              </span>
            </div>

            <div class="pt-2">
              <div class="flex items-center">
                <span class="text-gray-400 w-36 shrink-0">Tech</span>
                <div class="flex flex-wrap gap-2" role="list" aria-label="Tech">
                  {
                    Object.keys(techStack).map((lang) => (
                      <span
                        class="text-gray-400 hover:text-cyan-600 transition-colors"
                        title={techStack[lang]}
                      >
                        {lang}
                      </span>
                    ))
                  }
                </div>
              </div>
            </div>
          </div>
        </div>

        <div class="mt-3 pt-2 border-t border-gray-700 text-gray-500 text-xs">
          <span class="text-gray-400">$</span> cat about.txt
        </div>
      </div>
    </section>

    <section id="projects" class="mt-2">
      <div
        class="bg-black/20 border border-gray-700 rounded font-mono text-sm text-gray-300 p-6"
        role="region"
        aria-label="Showcase de proyectos"
      >
        <h2 class="mb-4 text-sm font-bold text-gray-500">💻 mis proyectos</h2>
        <div class="space-y-2" role="list">
          {
            projects.map((project, index) => (
              <div
                class="group hover:bg-gray-800/50 p-1 rounded transition-colors"
                role="listitem"
              >
                {project.link ? (
                  <a
                    href={project.link}
                    target="_blank"
                    rel="noopener noreferrer"
                    class="block focus:outline-none focus:ring-2 focus:ring-cyan-600 focus:ring-offset-2 focus:ring-offset-gray-900 rounded"
                    aria-label={`Visitar ${project.title}: ${project.description}`}
                  >
                    <div class="flex items-start gap-2">
                      <span
                        class="text-gray-400 w-6 shrink-0"
                        aria-hidden="true"
                      >
                        {String(index + 1).padStart(2, "0")}.
                      </span>
                      <span
                        class="text-yellow-360 shrink-0"
                        role="img"
                        aria-label="Icono del proyecto"
                      >
                        {project.icon}
                      </span>
                      <div class="min-w-0 flex-1">
                        <span class="text-white group-hover:text-cyan-300 font-semibold">
                          {project.title}
                        </span>
                        <span class="text-gray-500 ml-2">
                          → {project.description}
                        </span>
                      </div>
                    </div>
                  </a>
                ) : (
                  <div
                    class="flex items-start gap-2 opacity-60"
                    aria-label={`${project.title}: ${project.description} (Próximamente)`}
                  >
                    <span class="text-gray-400 w-6 shrink-0" aria-hidden="true">
                      {String(index + 1).padStart(2, "0")}.
                    </span>
                    <span
                      class="text-yellow-360 shrink-0"
                      role="img"
                      aria-label="Icono del proyecto"
                    >
                      {project.icon}
                    </span>
                    <div class="min-w-0 flex-1">
                      <span class="text-white font-semibold">
                        {project.title}
                      </span>
                      <span class="text-gray-500 ml-2">
                        → {project.description}
                      </span>
                      <span class="text-gray-600 ml-2">[próximamente]</span>
                    </div>
                  </div>
                )}
              </div>
            ))
          }
        </div>
        <div class="mt-3 pt-2 border-t border-gray-700 text-gray-500 text-xs">
          <span class="text-gray-400">$</span> ls -la proyectos/ | total {
            projects.length
          }
        </div>
      </div>
    </section>

    <section id="blog" class="mt-8">
      <h2 class="text-sm mb-6 font-semibold text-gray-500">✍️ últimos posts</h2>
      <ul class="space-y-6" role="list">
        {
          allPostsByDate.map((post) => (
            <li class="flex flex-col gap-x-2 sm:flex-row" role="listitem">
              <PostPreview post={post} />
            </li>
          ))
        }
      </ul>
    </section>
  </main>
</BaseLayout>
