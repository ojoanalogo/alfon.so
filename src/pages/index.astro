---
import { getCollection } from "astro:content";
import PostPreview from "../components/PostPreview.astro";
import SocialMediaIcons from "../components/SocialMediaIcons.astro";
import MatrixBackground from "../components/MatrixBackground.astro";
import BaseLayout from "../layouts/BaseLayout.astro";
import { sortMDByDate } from "../utils";
import me from "../assets/me.jpg";
import { Image } from "astro:assets";

const MAX_POSTS = 10;
const allPosts = await getCollection("blog");
const allPostsByDate = sortMDByDate(allPosts).slice(0, MAX_POSTS);

const techStack: Record<string, string> = {
  js: "JavaScript",
  ts: "TypeScript",
  "node.js": "Node",
  "astro": "This site is built on astro",
  python: "Python",
  sql: "SQL",
  "claude-code": "Best tool ever",
  "next.js": "Next.js",
  aws: "AWS",
  gcp: "GCP",
  docker: "Docker",
  tf: "Terraform",
};

const projects: Array<{
  title: string;
  description: string;
  link: string;
  icon: string;
}> = [
  {
    title: "sofia",
    description: "registra tus gastos con un solo mensaje de WhatsApp.",
    link: "https://sofinanzas.mx",
    icon: "💸",
  },
  {
    title: "terminus",
    description:
      "plantilla de Astro para sitios web personales, inspirada en una terminal de comandos.",
    link: "https://github.com/ojoanalogo/terminus-astro-template",
    icon: "💻",
  },
  {
    title: "hilitos",
    description: "marketplaces para pequeños emprendedores, como Etsy.",
    link: "https://hilitos.app",
    icon: "🧵",
  },
  {
    title: "dotfiles",
    description: "dale un vistazo a la configuración de mi equipo personal.",
    link: "https://github.com/ojoanalogo/dotfiles",
    icon: "🔧",
  },
  {
    title: "wawa",
    description:
      "framework para construir flujos conversacionales en whatsapp. [próximamente]",
    icon: "💬",
    link: "",
  },
];
---

<BaseLayout navbarActiveItemID="home">
  <MatrixBackground
    slot="background"
    highlightColor="rgba(20, 184, 166, 0.25)"
    boxSize={40}
    movementSpeed={0.1}
    movementDirection="up-left"
  />
  <main>
    <section id="about" class="mt-6" aria-labelledby="about-heading">
      <div
        class="bg-white/50 dark:bg-black/10 backdrop-blur-lg border border-gray-300/40 dark:border-gray-600/40 rounded-lg font-mono text-sm p-4 sm:p-6 transition-all duration-300 hover:border-gray-400/50 dark:hover:border-gray-400/30 hover:shadow-sm dark:hover:shadow-md dark:hover:shadow-black/20"
        role="region"
      >
        <!-- Terminal header -->
        <div class="flex items-center gap-2 mb-4 pb-3 border-b border-gray-300 dark:border-gray-600">
          <div class="flex gap-2" role="img">
            <div class="w-3 h-3 rounded-full bg-red-500"></div>
            <div class="w-3 h-3 rounded-full bg-yellow-500"></div>
            <div class="w-3 h-3 rounded-full bg-green-500"></div>
          </div>
          <span class="text-muted text-xs ml-2 hidden sm:inline"
            >terminal - ssh guest@alfon.so</span
          >
        </div>

        <!-- Content -->
        <div class="text-xs">
          <div class="space-y-4 sm:space-y-2">
            <div class="flex items-center gap-3 mb-4">
<!--               <Image
                src={me}
                width={180}
                alt="Avatar de Alfonso"
                class="w-16 h-16 rounded-full hover:scale-105 transition-transform drop-shadow-xl shrink-0"
              /> -->
              <div class="flex flex-col gap-1">
                <h1 class="text-xl">
                  👋 hola soy <span class="text-primary font-medium">alfonso reyes</span>
                </h1>
                <p class="text-muted text-xs">
                  ingeniero backend / fotógrafo
                </p>
              </div>
            </div>

            <!-- Personal paragraph -->
            <div class="bg-gray-200/50 dark:bg-gray-500/10 rounded-lg p-3 mb-4">
              <p class="text-primary leading-relaxed">
                bienvenido a mi pequeño rincón en internet, aquí encontrarás mis
                pensamientos (pocos), mis proyectos y mi blog.
              </p>
            </div>

            <div class="border-b border-gray-300 dark:border-gray-600 w-full mb-2"></div>

            <div class="flex flex-col sm:flex-row sm:items-center gap-0.5 sm:gap-0">
              <span class="text-muted sm:w-36 shrink-0">ubicación</span>
              <span class="text-secondary">méxico 🌮🇲🇽</span>
            </div>

            <div class="flex flex-col sm:flex-row gap-0.5 sm:gap-0">
              <span class="text-muted sm:w-36 shrink-0">trabajo</span>
              <span class="text-secondary">
                ingeniero backend @
                <a
                  target="_blank"
                  rel="noopener noreferrer"
                  class="text-accent hover:underline focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-2 focus:ring-offset-background"
                  href="https://monopolio.com.mx"
                  aria-label="Visitar sitio web monopolio.com.mx"
                  >monopolio.com.mx</a
                >
              </span>
            </div>

            <div class="flex flex-col sm:flex-row gap-0.5 sm:gap-0">
              <span class="text-muted sm:w-36 shrink-0">hobby</span>
              <span class="text-secondary">
                fotografía @
                <a
                  class="text-accent hover:underline focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-2 focus:ring-offset-background"
                  target="_blank"
                  rel="noopener noreferrer"
                  href="https://ojoanalogo.com"
                  aria-label="Visitar portafolio de fotografía ojoanalogo.com"
                  >ojoanalogo.com</a
                >
              </span>
            </div>

            <div class="flex flex-col sm:flex-row gap-0.5 sm:gap-0">
              <span class="text-muted sm:w-36 shrink-0">proyecto actual</span>
              <span class="text-secondary">
                <a
                  target="_blank"
                  rel="noopener noreferrer"
                  class="text-accent hover:underline focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-2 focus:ring-offset-background"
                  href="https://sofinanzas.mx"
                  aria-label="Visitar SofIA - Asistente de finanzas con IA"
                  >sofia</a
                >
                - asistente de finanzas personales para todos 💸
              </span>
            </div>

            <div class="flex flex-col sm:flex-row gap-0.5 sm:gap-0">
              <span class="text-muted sm:w-36 shrink-0">intereses</span>
              <span class="text-secondary"
                >programación, startups, ciencia ficción, películas, fotografía,
                viajes</span
              >
            </div>

            <div class="flex flex-col sm:flex-row gap-0.5 sm:gap-0">
              <span class="text-muted sm:w-36 shrink-0">contacto</span>
              <span class="text-secondary">
                <a
                  class="text-accent hover:underline focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-2 focus:ring-offset-background"
                  href="mailto:hola@alfon.so"
                  aria-label="Enviar email a hola@alfon.so">hola@alfon.so</a
                >
              </span>
            </div>

            <div class="flex flex-col sm:flex-row gap-0.5 sm:gap-0">
              <span class="text-muted sm:w-36 shrink-0">social</span>
              <span class="text-secondary flex-1 min-w-0">
                <SocialMediaIcons />
              </span>
            </div>

            <div class="pt-2">
              <div class="flex flex-col sm:flex-row gap-0.5 sm:gap-0">
                <span class="text-muted sm:w-36 shrink-0">tech stack</span>
                <div class="flex flex-wrap gap-2" role="list" aria-label="Tech stack">
                  {
                    Object.keys(techStack).map((lang) => (
                      <span
                        class="text-secondary hover:text-accent transition-colors"
                        title={techStack[lang]}
                      >
                        {lang}
                      </span>
                    ))
                  }
                </div>
              </div>
            </div>
          </div>
        </div>

        <div class="mt-3 pt-2 border-t border-gray-300 dark:border-gray-700 text-muted text-xs">
          <span class="text-secondary">$</span> cat about.txt
        </div>
      </div>
    </section>

    <section id="projects" class="mt-2">
      <div
        class="bg-white/50 dark:bg-black/10 backdrop-blur-lg border border-gray-300/40 dark:border-gray-600/40 rounded-lg font-mono text-sm text-secondary p-4 sm:p-6 transition-all duration-300 hover:border-gray-400/50 dark:hover:border-gray-400/30 hover:shadow-sm dark:hover:shadow-md dark:hover:shadow-black/20"
        role="region"
        aria-label="Showcase de proyectos"
      >
        <h2 class="mb-4 text-sm font-bold text-muted">💻 mis proyectos</h2>
        <div class="space-y-2" role="list">
          {
            projects.map((project, index) => (
              <div
                class="group hover:bg-gray-200/50 dark:hover:bg-gray-900/40 p-1 rounded transition-colors"
                role="listitem"
              >
                {project.link ? (
                  <a
                    href={project.link}
                    target="_blank"
                    rel="noopener noreferrer"
                    class="block focus:outline-none focus:ring-2 focus:ring-teal-800 focus:ring-offset-2 focus:ring-offset-background rounded"
                    aria-label={`Visitar ${project.title}: ${project.description}`}
                  >
                    <div class="flex items-start gap-2">
                      <span
                        class="text-muted w-6 shrink-0"
                        aria-hidden="true"
                      >
                        {String(index + 1).padStart(2, "0")}.
                      </span>
                      <span
                        class="text-yellow-360 shrink-0"
                        role="img"
                        aria-label="Icono del proyecto"
                      >
                        {project.icon}
                      </span>
                      <div class="min-w-0 flex-1">
                        <span class="text-secondary group-hover:text-accent font-semibold block sm:inline">
                          {project.title}
                        </span>
                        <span class="text-muted sm:ml-2 block sm:inline mt-1 sm:mt-0">
                          → {project.description}
                        </span>
                      </div>
                    </div>
                  </a>
                ) : (
                  <div
                    class="flex items-start gap-2 opacity-60"
                    aria-label={`${project.title}: ${project.description} (Próximamente)`}
                  >
                    <span class="text-muted w-6 shrink-0" aria-hidden="true">
                      {String(index + 1).padStart(2, "0")}.
                    </span>
                    <span
                      class="text-yellow-360 shrink-0"
                      role="img"
                      aria-label="Icono del proyecto"
                    >
                      {project.icon}
                    </span>
                    <div class="min-w-0 flex-1">
                      <span class="text-secondary font-semibold block sm:inline">
                        {project.title}
                      </span>
                      <span class="text-muted sm:ml-2 block sm:inline mt-1 sm:mt-0">
                        → {project.description}
                      </span>
                      <span class="text-muted/70 sm:ml-2 block sm:inline">[próximamente]</span>
                    </div>
                  </div>
                )}
              </div>
            ))
          }
        </div>
        <div class="mt-3 pt-2 border-t border-gray-300 dark:border-gray-700 text-muted text-xs">
          <span class="text-secondary">$</span> ls -la proyectos/ | total {
            projects.length
          }
        </div>
      </div>
    </section>

    {
      allPosts.length > 0 && (
        <section id="blog" class="mt-2">
          <div
            class="bg-white/50 dark:bg-black/10 backdrop-blur-lg border border-gray-300/40 dark:border-gray-600/40 rounded-lg font-mono text-sm p-4 sm:p-6 transition-all duration-300 hover:border-gray-400/50 dark:hover:border-gray-400/30 hover:shadow-sm dark:hover:shadow-md dark:hover:shadow-black/20"
          >
            <h2 class="text-sm mb-4 font-bold text-muted">
              ✍️ últimos posts
            </h2>
            <ul class="space-y-4 sm:space-y-2" role="list">
              {allPostsByDate.map((post) => (
                <li class="flex flex-col gap-x-2 sm:flex-row" role="listitem">
                  <PostPreview post={post} />
                </li>
              ))}
            </ul>
            <div class="mt-3 pt-2 border-t border-gray-300 dark:border-gray-700 text-muted text-xs">
              <span class="text-secondary">$</span> tail -n {allPostsByDate.length} blog/
            </div>
          </div>
        </section>
      )
    }
  </main>
</BaseLayout>
