---
interface Props {
  label: string;
}

const { label } = Astro.props;
---

<div class="flex flex-col gap-0.5 sm:flex-row sm:items-center sm:gap-0">
  <span class="shrink-0 text-muted sm:w-36">{label}</span>
  <span class="text-secondary"><slot /></span>
</div>
