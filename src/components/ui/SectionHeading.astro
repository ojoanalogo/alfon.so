---
interface Props {
  as?: 'h1' | 'h2' | 'h3';
  class?: string;
}

const { as: Tag = 'h2', class: className } = Astro.props;
---

<Tag class:list={['mb-3 text-sm font-bold text-muted', className]}>
  <slot />
</Tag>
