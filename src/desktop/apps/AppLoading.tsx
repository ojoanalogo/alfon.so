/** Minimal body fallback shown while a code-split app chunk loads on first open. */
export default function AppLoading() {
  return (
    <div className="flex min-h-0 flex-1 items-center justify-center p-6 text-xs text-muted">
      cargando…
    </div>
  );
}
